// Declares variables
let numOne = 6;
let numTwo = 2;
let result = numOne - numTwo;
// Prints message to display values. 
// Using absolute value for conditions where numTwo is greater than numOne(Avoid negative numbers)
console.log("The difference between " + numOne + " and " + numTwo + " is " + Math.abs(result));
